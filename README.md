# 01 - First API Endpoint

Small Express server with two endpoints.

- GET / -> returns a hello message
- GET /status -> returns status + current time

## Run it
npm install
node server.js

Then test with curl or in browser at localhost:5000
